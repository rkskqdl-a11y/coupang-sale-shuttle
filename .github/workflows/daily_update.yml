name: Scheduled Daily Automation and Manual Dispatch

on:
  schedule:
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  automation-engine:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout Codebase
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 1

      - name: Configure Python Environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          # 💎 cache: 'pip' 줄을 삭제하여 에러를 방지했습니다.

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # 💎 복잡한 AI 라이브러리 대신, 가장 기본적이고 튼튼한 requests만 사용합니다.
          pip install requests
      - name: Execute Automation Script
        env:
          COUPANG_ACCESS_KEY: ${{ secrets.COUPANG_ACCESS_KEY }}
          COUPANG_SECRET_KEY: ${{ secrets.COUPANG_SECRET_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          echo "🚀 포스팅 생성을 시작합니다..."
          # 💎 아래 한 줄이 반드시 있어야 로봇이 작동합니다!
          python main.py
      - name: Synchronize Changes to Repository
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          if ! git diff-index --quiet HEAD; then
            # 💎 [중요] [skip ci]를 삭제하여 웹사이트가 자동 배포되게 합니다.
            git commit -m "chore: 일일 자동 업데이트 ($(date +'%Y-%m-%d'))"
            git push
          else
            echo "변경 사항 없음"
          fi
        shell: bash
