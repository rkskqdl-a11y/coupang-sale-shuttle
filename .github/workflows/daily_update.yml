name: Coupang Partners Automation

on:
  schedule:
    # 한국 시간 기준: 09시, 15시, 21시, 03시 (UTC 0, 6, 12, 18)
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch: # 수동 실행 버튼

permissions:
  contents: write

jobs:
  automation-engine:
    runs-on: ubuntu-latest
    # 💎 6시간 타임아웃 방지를 위해 최대 실행 시간을 60분으로 제한합니다.
    timeout-minutes: 60 

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run automation script
        env:
          COUPANG_ACCESS_KEY: ${{ secrets.COUPANG_ACCESS_KEY }}
          COUPANG_SECRET_KEY: ${{ secrets.COUPANG_SECRET_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        # 💎 -u 옵션을 추가하여 실시간으로 로그가 출력되게 합니다.
        run: python -u main.py

      - name: Commit and push changes
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          # 변경사항이 있을 때만 커밋 (에러 방지)
          if ! git diff-index --quiet HEAD; then
            git commit -m "Auto-update: New expert reviews $(date +'%Y-%m-%d')"
            git push
          else
            echo "No changes detected. Skipping push."
          fi
